# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# deploy/rebuild.sh stages a build here, then keeps the previous one for rollback
dist.new
dist.old
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SSR bundle — an intermediate of the prerender, not a deliverable
dist-server
